import * as acions from "../actions/"
export const initalState ={
  loading:false,
  articles:[]
}

 const articleReducer = (state = initalState, action)=>{
  switch(action.type)
  {
    case :
      return

    default :
    return state;
  }
}